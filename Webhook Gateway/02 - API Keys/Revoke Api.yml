info:
  name: Revoke Api
  type: http
  seq: 3

http:
  method: DELETE
  url: http://localhost:8080/api/v1/api-keys
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
